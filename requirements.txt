# FlowState Runtime Dependencies
# Python 3.9+ required
# For development dependencies, see requirements-dev.txt

# NFC Reader
pn532pi==1.7

# Configuration
PyYAML==6.0.1

# GPIO for buzzer/LED (only needed on Pi)
RPi.GPIO==0.7.1

# Web server (dashboards, API, control panel)
Flask==3.0.0

# HTTP requests (for peer station monitoring)
requests==2.32.3

# NDEF writing (for NFC card initialization)
ndeflib==0.3.3
