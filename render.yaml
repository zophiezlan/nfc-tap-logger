services:
  - type: web
    name: nfc-tap-logger-demo
    env: python
    region: oregon
    plan: free
    branch: claude/setup-demo-site-TAgnt
    buildCommand: pip install -r requirements-web.txt
    startCommand: python demo_server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
      - key: DEMO_MODE
        value: "true"
